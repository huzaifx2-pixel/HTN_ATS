# dependencies
node_modules/
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build
/dist
/desktop/dist

# desktop bundled postgres (large binaries — see resources/postgresql/README.md)
/resources/postgresql/bin/
/resources/postgresql/lib/
/resources/postgresql/share/

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*
*.log
.build-log.txt

# editor
.cursor/

# env files and secret backups (never commit)
.env
.env.*
!.env.example
!.env.neon.example
!.env.production.example
.env*.local
.env*.bak

# uploads (legacy — production uses ./data/uploads)
/uploads
/data

# typescript
*.tsbuildinfo
next-env.d.ts

# prisma
/prisma/migrations/**/migration.sql.bak

# local databases (over GitHub size limit; never commit)
*.db
*.db-journal
*.db-wal
*.db-shm

# reference clones (not part of the ATS app)
/htn-api-ref/
/headsbase-careers-ref/

# accidental nested copy of this repo
/HTN_ATS/
